import React from "react";

/* Create a Note.js component to show a <div> element with a
 <h1> for a title and a <p> for the content en geef de div een className = "note"
 */

const Note = () => {
  return (
    <div className="note">
      <h1>Titel van de Note</h1>
      <p>Content van de Note</p>
    </div>
  );
};

export default Note;
