import React from "react";

/* Create a Header.js component that renders a <header> element to show the Keeper App name in an <h1>.
 */
const Header = () => {
  return (
    <header>
      <h1>Keeper App</h1>
    </header>
  );
};

export default Header;
